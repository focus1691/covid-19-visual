export const chartStyle = theme => ({
  formControl: {
    margin: theme.spacing(1),
    padding: theme.spacing(2),
    '& select': {
      color: 'red'
    }
  },
  select: {
    padding: theme.spacing(2),
  },
});